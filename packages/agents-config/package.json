{
  "name": "@digitsoft-ai/agents-config",
  "version": "1.0.0",
  "private": true,
  "description": "Agent configurations for DigitSoft AI",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src --ext .ts,.json",
    "format": "prettier --write src/**/*.{ts,json}",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@digitsoft-ai/shared-types": "workspace:*",
    "typescript": "^5.3.0",
    "eslint": "^8.50.0",
    "prettier": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
