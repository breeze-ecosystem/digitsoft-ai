{
  "name": "digitsoft-ai",
  "version": "1.0.0",
  "private": true,
  "description": "DigitSoft AI - Alternative africaine auto-hébergée à Claude/ChatGPT",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "format": "turbo run format",
    "type-check": "turbo run type-check"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
